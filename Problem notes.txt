The real problem is that the generator only adds travel constraints for explicitly dependent tasks, not for consecutive tasks of the same agent that lack dependencies. To fix this, I'd need to either know the task order at generation time (which the solver determines) or add travel constraints for all possible orderings, both of which are problematic. Looking at the actual output, Harvest_Row1 ends at t=3 at location l1, and Harvest_Row2 starts immediately at t=3 at l3 with zero travel time—human1 can't teleport between those locations.

Human1 would need 3 minutes to travel from l1 to l3 (l1→l2 is 1 min, l2→l3 is 2 min), so the schedule is infeasible. The model allows this because there's no explicit dependency forcing a travel constraint between the two harvest tasks.

The real issue is that the generator only adds travel time for explicitly dependent tasks or initial positioning—it doesn't account for travel between same-agent tasks that are merely ordered by no-overlap constraints. Since the user intentionally made the rows independent (depends_on: []), the scheduler treats them as potentially parallel, but with only one human available, they must be sequential with travel time between them.

To fix this properly, I'd need to add conditional constraints for all possible task orderings of the same agent, using a BoolVar to determine which task comes first and then enforcing the appropriate travel-time constraint. But that's complex. For now, the simpler solution might be for the user to add explicit dependencies in the JSON, or we build this as a generator feature. I could introduce a BoolVar for each pair of same-agent tasks to determine their ordering, then add constraints for travel time based on which task comes first—this is the standard approach for sequence-dependent setup times. The downside is that this creates O(n²) variables and constraints, so for a human with 12 tasks across 4 rows, I'd be adding 66 pairs worth of overhead. For mandatory tasks this is necessary, but optional tasks complicate things further.

